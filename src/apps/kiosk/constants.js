/**
 * Kiosk App Constants
 * Single point of import for Kiosk module - uses unified constants
 *
 * USAGE:
 *   import { API, TIMING, ORDER_TYPES } from './constants'
 */

export {
  API,
  TIMING,
  UI,
  ORDER_TYPES,
  ORDER_STATUS,
  ITEM_MODES,
  MODULE_ICONS,
  MODULE_COLORS,
  MODULE_PRIORITY,
} from '@/shared/api/constants'
